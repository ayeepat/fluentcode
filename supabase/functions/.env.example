# Configure these as Supabase Edge Function secrets; never expose them as VITE_* variables.
GROQ_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
CLERK_JWT_ISSUER=https://your-clerk-instance.clerk.accounts.dev
# Required only when your Clerk JWT template has an audience claim.
CLERK_JWT_AUDIENCE=
